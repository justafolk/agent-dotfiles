# Agent Worktree Orchestration

Use `AGENTS.md` as the canonical instruction file for Codex, Claude Code, and OpenCode worktree orchestration. It defines how to read `~/todo.md`, group related items, create efficient worktrees, and title tmux windows with `ai-worktree-create`.
